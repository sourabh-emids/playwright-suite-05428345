# Copy this file to .env and fill in real values. .env is gitignored --
# never commit credentials.

# The base URL every test navigates against. Required.
BASE_URL=https://www.emids.com

# The test account this suite signs in with. Leave both blank if the
# application under test needs no sign-in.
TEST_USERNAME=
TEST_PASSWORD=
